# Transforms vectors between image coordinates system(ICS) and world coordinate system (WGS)
#
# ICS is a 2D Cartesian coordinate system, where coordinates are measured in pixels.
# WGS is a 3D right-handed Cartesian coordinate system, where coordinates are measures in meters (or centimeters?)


def transform_ICS_to_WGS(x):
    """
    
    :param x: 2D point in ICS
    :return: 3D point in WGS
    """

    x_3d = []
    return x_3d
